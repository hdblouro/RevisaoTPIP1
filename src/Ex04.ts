let inputs:Array<string|number> = []; // Aceitará elementos string ou number

inputs[0] = 1;
inputs = ["a","b"];
inputs = [1,2,3];
inputs = ["a",2];
inputs = [true]; // Errado: o Array inputs não aceita boolean

export default {};